import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '../ui/card';

const AdminMemberManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Member Administration</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Member administration coming soon...</p>
      </CardContent>
    </Card>
  );
};

export default AdminMemberManager;
