import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '../ui/card';

const ClubInfoManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Club Information</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Club information management coming soon...</p>
      </CardContent>
    </Card>
  );
};

export default ClubInfoManager;
